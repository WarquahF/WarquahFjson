---
import { social } from '../data/nav';
---

<section class="section" id="contact" data-reveal style="border-bottom: none;">
  <div class="section-inner">
    <p class="eyebrow"><span class="path">/contact</span></p>
    <h2 data-text-reveal>Get in touch</h2>
    <p style="margin-top: 0.5rem;">
      Open to projects, collaborations and conversations about software
      and systems.
    </p>

    <div class="contact-links mono">
      <a href={social.github} target="_blank" rel="noreferrer">
        <span class="contact-label">GitHub</span>
        <span class="contact-value">github.com/WarquahF</span>
      </a>
      <a href={`mailto:${social.email}`}>
        <span class="contact-label">Email</span>
        <span class="contact-value">{social.email}</span>
      </a>
      <a href={social.x} target="_blank" rel="noreferrer">
        <span class="contact-label">X</span>
        <span class="contact-value">@warquah35</span>
      </a>
      <!-- TODO(placeholder): no résumé file yet — this row points at email.
           Swap the href for a real file/URL (e.g. /resume.pdf) once one exists. -->
      <a href={`mailto:${social.email}`}>
        <span class="contact-label">Résumé — email for now</span>
        <span class="contact-value">{social.email}</span>
      </a>
    </div>

    <p class="contact-note mono">Prefer async? Email works best — I usually reply within 24 hours.</p>
  </div>
</section>

<style>
  .contact-links {
    margin-top: var(--space-xl);
    display: flex;
    flex-direction: column;
    border-top: 1px solid var(--line);
  }

  .contact-links a {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.9rem var(--space-2xs);
    border-bottom: 1px solid var(--line);
    text-decoration: none;
    transition: padding-left 0.25s var(--ease-out), color 0.2s var(--ease-out);
    color: var(--ink-dim);
  }

  .contact-links a:hover {
    padding-left: 0.6rem;
    color: var(--ink);
  }

  .contact-label {
    font-size: var(--step--1);
    color: var(--ink-faint);
  }

  .contact-value {
    font-size: var(--step--1);
    transition: color 0.2s var(--ease-out);
  }

  .contact-links a:hover .contact-value {
    color: var(--signal);
  }

  .contact-note {
    margin-top: var(--space-lg);
    font-size: var(--step--1);
    color: var(--ink-faint);
  }
</style>
